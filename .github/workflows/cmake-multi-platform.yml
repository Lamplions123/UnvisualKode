name: CMake Multi-Platform Build

on:
  push:
    branches: [ "main", "master", "develop" ]
  pull_request:
    branches: [ "main", "master", "develop" ]

env:
  BUILD_TYPE: Release
  QT_VERSION: 6.5.3

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        include:
          - os: ubuntu-latest
            triplet: x64-linux
            qt_arch: gcc_64
            artifact_name: UnvisualKode-Linux
          - os: windows-latest
            triplet: x64-windows
            qt_arch: win64_msvc2019_64
            artifact_name: UnvisualKode-Windows
          - os: macos-latest
            triplet: x64-osx
            qt_arch: clang_64
            artifact_name: UnvisualKode-macOS

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Install Linux dependencies
      if: runner.os == 'Linux'
      run: |
        sudo apt-get update
        sudo apt-get install -y \
          libgl1-mesa-dev \
          libglu1-mesa-dev \
          libxkbcommon-dev \
          libxkbcommon-x11-dev \
          libxcb-xinerama0-dev \
          libxcb-icccm4-dev \
          libxcb-image0-dev \
          libxcb-keysyms1-dev \
          libxcb-render-util0-dev \
          libxcb-shape0-dev \
          libxcb-xfixes0-dev \
          libxcb-randr0-dev \
          libxcb-sync-dev \
          libxcb-shm0-dev \
          libfontconfig1-dev \
          libfreetype6-dev \
          libx11-dev \
          libx11-xcb-dev \
          libxext-dev \
          libxfixes-dev \
          libxi-dev \
          libxrender-dev \
          libxrandr-dev \
          libxss-dev \
          libdbus-1-dev \
          libatspi2.0-dev \
          ninja-build

    - name: Install Qt
      uses: jurplel/install-qt-action@v4
      with:
        version: ${{ env.QT_VERSION }}
        arch: ${{ matrix.qt_arch }}
        cache: true
        cache-key-prefix: qt-${{ matrix.os }}-${{ env.QT_VERSION }}

    - name: Setup Ninja (macOS)
      if: runner.os == 'macOS'
      uses: seanmiddleditch/gha-setup-ninja@v4

    - name: Setup MSVC (Windows)
      if: runner.os == 'Windows'
      uses: ilammy/msvc-dev-cmd@v1

    - name: Configure CMake (Linux/macOS)
      if: runner.os != 'Windows'
      run: |
        cmake -B build -G Ninja \
          -DCMAKE_BUILD_TYPE=${{ env.BUILD_TYPE }} \
          -DCMAKE_PREFIX_PATH=${{ env.Qt6_DIR }}

    - name: Configure CMake (Windows)
      if: runner.os == 'Windows'
      shell: pwsh
      run: |
        $qtDir = "${{ env.Qt6_DIR }}"
        if (-not $qtDir) {
          $qtDir = "${{ runner.temp }}/Qt/${{ env.QT_VERSION }}/msvc2019_64"
        }
        Write-Host "Qt6_DIR: $qtDir"
        cmake -B build -G "Visual Studio 17 2022" -A x64 `
          -DCMAKE_BUILD_TYPE=${{ env.BUILD_TYPE }} `
          -DCMAKE_PREFIX_PATH="$qtDir"

    - name: Build
      run: cmake --build build --config ${{ env.BUILD_TYPE }} --parallel

    - name: Install
      run: |
        cmake --install build --prefix install --config ${{ env.BUILD_TYPE }}

    - name: Package (Linux)
      if: runner.os == 'Linux'
      run: |
        cd install
        tar -czvf ../UnvisualKode-Linux.tar.gz .

    - name: Package (macOS)
      if: runner.os == 'macOS'
      run: |
        cd install
        zip -r ../UnvisualKode-macOS.zip .

    - name: Package (Windows)
      if: runner.os == 'Windows'
      run: |
        Compress-Archive -Path install/* -DestinationPath UnvisualKode-Windows.zip

    - name: Upload artifact
      uses: actions/upload-artifact@v4
      with:
        name: ${{ matrix.artifact_name }}
        path: |
          UnvisualKode-Linux.tar.gz
          UnvisualKode-macOS.zip
          UnvisualKode-Windows.zip
        if-no-files-found: warn

    - name: Upload build logs on failure
      if: failure()
      uses: actions/upload-artifact@v4
      with:
        name: build-logs-${{ matrix.os }}
        path: |
          build/CMakeFiles/CMakeOutput.log
          build/CMakeFiles/CMakeError.log

  release:
    needs: build
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && contains(github.ref, 'refs/tags/v')
    permissions:
      contents: write

    steps:
    - name: Download all artifacts
      uses: actions/download-artifact@v4
      with:
        path: artifacts

    - name: Create GitHub Release
      uses: softprops/action-gh-release@v1
      with:
        files: |
          artifacts/UnvisualKode-Linux/UnvisualKode-Linux.tar.gz
          artifacts/UnvisualKode-macOS/UnvisualKode-macOS.zip
          artifacts/UnvisualKode-Windows/UnvisualKode-Windows.zip
        generate_release_notes: true
        draft: false
        prerelease: false
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}